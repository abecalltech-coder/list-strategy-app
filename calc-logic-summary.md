# Listgram 集計タブ 数字の出どころ・計算方法まとめ

対象: 「集計」タブ(行=リスト名/行=業種)と「クール戦略」タブ。
2026年9月時点の実装(app.js)をもとにまとめています。

## 元になるシート(エリアごとに5枚)

| シート名の例 | 役割 |
|---|---|
| 【エリア】リストデータ | A列=リスト名、B列=都道府県、D列=未コール(生の値)、F列以降=架電結果のカテゴリ別列 |
| 【エリア】業種未コール | 業種の内訳列 + 末尾に「合計」列 |
| 【エリア】業種不在1 | 同上 |
| 【エリア】業種不在2 | 同上 |
| 【エリア】業種不在3以上 | 同上 |

シート名に含まれる文字列(「リストデータ」「業種未コール」など)で自動判別しているため、列の並び順が変わっても対応できます。

---

## 行=リスト名モードの各項目

### 未コール・不在1・不在2・不在3以上
- **出どころ**: 対応する業種別シート(業種未コール/業種不在1/業種不在2/業種不在3以上)の一番右の「合計」列
- **計算**: リスト名ごとに、都道府県の行を合算しただけの単純合計(都道府県フィルターがかかっていれば対象都道府県の行だけ)

### 架電可能数
- **計算**: 「表示する項目を選択」でON(表示中)になっている「未コール・不在1・不在2・不在3以上」だけの合計
- 1つも選んでいない場合は4項目全部を合計
- クール戦略タブでは常に4項目全部の合計固定(表示選択の影響を受けない)

### 有効結果
- **出どころ**: リストデータシートの中で、下記22個の列名に**完全一致**する列(位置・並び順は問わない)
- **計算**: 該当する列をすべて合計。1つでも見つからなければその分は0扱い
- 「不在」という列名は対象から**除外**

対象の列名(22個):
決裁者不在 / アプローチNG / 主旨NG / クロージングNG / 電気NG / SMSNG / トスアップ / アポイント / 見込みC / 見込みC(不在) / 見込みB / 見込みB(不在) / 見込みA / 見込みA(不在) / 19時以降対応案件 / 土日架電希望案件 / 対象外(既契約) / 対象外(建物管理) / 対象外(本社管理) / 対象外(オール電化・太陽光等) / 対象外(高圧) / 対象外(その他) / アポ禁

### 有効率
- **計算式**: 有効結果 ÷ (不在1 + 不在2 + 不在3以上 の合計 + 有効結果) × 100
- 分母が0のときは「—」表示

### トスアップ率(2026-09-21に計算方法を変更)
- **出どころ**: リストデータシートの中から、列名で自動選択した「トスアップ」列と「アポ」列(下表の条件で自動選択)
- **計算式**: (トスアップ列の値 + アポ列の値の合計) ÷ 有効結果 × 100(対有効)
- 旧計算式(トスアップ列の値のみ ÷ 有効結果)から、アポの件数も含める形に変更しました

### アポイント率・アプローチNG率・クロージングNG率
- **出どころ**: リストデータシートの中から、列名で自動選択した1列(下表参照)
- **計算式**: その列の値 ÷ 有効結果 × 100(いずれも「対有効」)

| 表示名 | 探す列名の条件 |
|---|---|
| トスアップ率(分子側) | 列名が「トスアップ」に完全一致、無ければ「トスアップ」を含む列 |
| アポイント率/トスアップ率(分子側) | 列名が「アポ」「アポイント」「アポ数」「獲得アポ」のいずれかに完全一致、無ければ「アポ」を含み「禁」を含まない列 |
| アプローチNG率 | 列名が「アプローチNG」に完全一致、無ければ「アプローチNG」または「アプローチ」を含む列 |
| クロージングNG率 | 列名が「クロージングNG」に完全一致、無ければ「クロージングNG」または「クロージング」を含む列 |

### 決裁者接触率(2026-09-14に計算方法を変更)
- **出どころ**: リストデータシートの中の「主旨NG」「クロージングNG」「電気NG」「SMSNG」「トスアップ」「アポイント」の6列(列名一致で自動選択。無ければ0扱い)
- **計算式**: (主旨NG + クロージングNG + 電気NG + SMSNG + トスアップ + アポイントの合計) ÷ リスト総数 × 100
- **リスト総数** = 未コール + 不在1 + 不在2 + 不在3以上(表示項目の選択に関係なく常に4項目全部) + 有効結果 の合計(=そのリストの全レコード数)
- 決裁者につながった(何らかの形で応対まで到達できた)割合を、リスト全体に対する比率として表す項目です

※ これらは有効結果とは別に、それぞれ1列だけを拾う仕組みです(有効結果の22列合計とは別ロジック)。

### リストデータ未コール(既定は非表示)
- **出どころ**: リストデータシートのD列の値をそのまま
- **計算**: リスト名ごとに単純合計
- **用途**: 業種未コールシート側の「未コール」(合計列)と本来一致するはずの数値を突き合わせ確認するための項目。業種未コール側の計算には一切影響しません
- 「表示する項目を選択」からONにすると「未コール」の隣に表示されます

### 業種:◯◯(業種名の列)
- **出どころ**: 業種別4シート(業種未コール+業種不在1+業種不在2+業種不在3以上)の、その業種の列
- **計算**: 4シート分のその業種の値をリスト名ごとに合計

### その他リストデータの項目
- **出どころ**: リストデータシートのF列以降で、上記のどの項目名にも一致しない列
- 値の下に、有効結果に対する割合もあわせて表示

---

## 行=業種モード

未コール・不在1・不在2・不在3以上・架電可能数のみ表示されます。業種別4シートを、業種名を基準に全リストを横断して合計したものです。有効結果やそれに連動する率(有効率・トスアップ率など)はこのモードには出ません。

---

## クール戦略タブ

集計タブと同じ「有効結果・有効率・トスアップ率・決裁者接触率・クロージングNG率」の定義をそのまま使います。違いは架電可能数が常に未コール〜不在3以上の4項目全部の合計固定である点だけです(集計タブのように表示項目選択の影響を受けません)。

---

## 都道府県フィルターについて

上部の「都道府県で絞り込み」で選択中の都道府県があると、上記すべての項目がその都道府県の行だけで再計算されます(架電可能数・有効結果・各種率もすべて対象都道府県分のみ)。
